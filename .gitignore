.env
.env.*
!.env.example
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
logs/
state/
state_backup/
backups/
data_store/
runtime/
pids/
*.pid
reports/runtime_snapshots/
*.bak*
*.backup*

# Generated reports / audits
reports/
full_tree.txt
project_inventory.txt
*.save
0
archive/
# Historical datasets
data/historical/
.worktrees/

# Per-mode configuration (RC2). The forward config and the live TEMPLATE carry no
# credentials and are tracked so the deployed configuration is auditable. The real
# .env.live and the owner authorisation token are never committed.
!.env.forward
!.env.live.example
.env.live
state/LIVE_PILOT_AUTHORISATION
